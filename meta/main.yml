galaxy_info:
  author: marica.antonacci@gmail.com
  description: Deploy Mesos master/slave
  company: INDIGO-DataCloud
  license: Apache
  min_ansible_version: 2.0
  platforms:
    - name: Ubuntu
      versions:
        - trusty
        - xenial
    - name: EL
      versions:
        - 7  
  galaxy_tags: 
   - mesos
 
dependencies:
  - src: indigo-dc.docker
    name: indigo-dc.docker
    version: v1.1.0-dodas-rc1
    docker_dns: 
      - "{{docker_bridge_ip_cidr.split('/')[0]}}"
      - "8.8.8.8"
    docker_dns_search: 
      - "service.{{ consul_domain }}"
